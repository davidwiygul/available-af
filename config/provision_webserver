# provision_webserver
#!/bin/bash

source provision.config

ssh -T -i $AWS_SSH_KEY ubuntu@$1 << HERE
    sudo apt-get update
    sudo apt-get install -y postgresql-client
    exit
HERE

./efs $1
./airflow $1
./daemonize webserver $1
